# studies

Labs:

1. There are two pointer in memory, needs to display all addresses between them;
2. Given a number of type long int, it is necessary to calculate the number of single bits, using the variable char*;
3. Given two pointers to the address range, it is necessary for each pair of adjacent addresses to find the number of single bit, standing on the same positions;
