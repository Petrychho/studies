# studies

Labs:

1. Write a function reverse (int * first, int * last);
2. Invert a dynamic array;
3. Given two ordered arrays, it is necessary to get the third, which will result from the merging of the first two;
4. Given an ordered array, needs to implement a binary search;
5. Find it in the array of a pair of numbers that result from multiplying give the maximum result;
